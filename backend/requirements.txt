# PRODUCTION Requirements - No Compilation Needed
# All packages use pre-built wheels only

# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0
pydantic==2.5.0
python-multipart==0.0.6
python-dotenv==1.0.0

# AI/ML dependencies
openai==1.3.5

# Pre-built scientific computing
numpy==1.24.3

# SpaCy WITHOUT compilation (using specific compatible versions)
spacy==3.7.2 --only-binary=:all:
blis==0.7.11 --only-binary=:all:
thinc==8.1.12 --only-binary=:all:
murmurhash==1.0.10 --only-binary=:all:
cymem==2.0.8 --only-binary=:all:
preshed==3.0.9 --only-binary=:all:

# SpaCy supporting packages
wasabi==1.1.2
srsly==2.4.8
catalogue==2.0.10
typer==0.9.0
click==8.1.7

# Text processing
contractions==0.1.73

# Document processing
python-docx==1.1.0
docx2txt==0.8

# Utility dependencies
aiofiles==0.24.0
requests==2.31.0

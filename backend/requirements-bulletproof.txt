# BULLETPROOF Requirements for Render
# Uses only pre-compiled wheels to avoid compilation issues

# Core FastAPI dependencies (stable versions)
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0
pydantic==2.5.0
python-multipart==0.0.6
python-dotenv==1.0.0

# AI/ML dependencies
openai==1.3.5

# Pre-compiled NumPy (essential for SpaCy)
numpy==1.24.3

# SpaCy with ONLY binary installations (no compilation)
--only-binary=all spacy==3.7.2
--only-binary=all spacy-legacy==3.0.12
--only-binary=all spacy-loggers==1.0.5

# Essential SpaCy dependencies (pre-compiled only)
--only-binary=all blis==0.7.11
--only-binary=all thinc==8.1.12
--only-binary=all murmurhash==1.0.10
--only-binary=all cymem==2.0.8
--only-binary=all preshed==3.0.9
--only-binary=all wasabi==1.1.2
--only-binary=all srsly==2.4.8
--only-binary=all catalogue==2.0.10
--only-binary=all typer==0.9.0
--only-binary=all click==8.1.7

# Text processing
contractions==0.1.73

# Document processing
python-docx==1.1.0
docx2txt==0.8

# Utility dependencies
aiofiles==0.24.0
requests==2.31.0

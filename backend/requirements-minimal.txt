# EMERGENCY FALLBACK - Minimal dependencies for fast deployment
# Use this if SpaCy continues to fail

# Core FastAPI dependencies ONLY
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0
pydantic==2.5.0
python-multipart==0.0.6
python-dotenv==1.0.0

# AI/ML dependencies
openai==1.3.5

# Basic text processing (no compilation required)
nltk==3.8.1
contractions==0.1.73

# Document processing
python-docx==1.1.0
docx2txt==0.8

# Utility dependencies
aiofiles==0.24.0
requests==2.31.0

# Basic scientific computing
numpy==1.24.3

# Note: SpaCy will be installed separately after basic app is running

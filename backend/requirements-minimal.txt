# EMERGENCY MINIMAL - Fastest deployment possible
# Only essential packages for basic API functionality

# Core FastAPI dependencies ONLY
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0
pydantic==2.5.0
python-multipart==0.0.6
python-dotenv==1.0.0

# AI dependency
openai==1.3.5

# Basic utilities
aiofiles==0.24.0
requests>=2.31.0

# Document processing (lightweight)
docx2txt==0.8

# Text processing (no dependencies)
contractions==0.1.73

# Note: NumPy and SpaCy will be installed at runtime if needed
